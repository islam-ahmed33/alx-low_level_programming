#include "main"
/**
 * add - function that adds two integers and returns the result.
 * @a: The 1st intger
 * @b: The 2nd intger
 * Return: The result of adding a and b
 *
 */
int add(int a, int b)
{
return (a + b);
}
